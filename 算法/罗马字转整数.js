let line;

let values = {};

function fromRome(string) {
  // let nums = string.match(/IV|IX|XL|XC|CD|CM|I|V|X|L|C|D|M/g);
  // let numsReg = ..
  let r = 0;
  let c = 0;
  // exec 有个从上次匹配继续查找
  while ((c = numsR)) {}
}
